<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
     >
    <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_rank_list"
            android:background="@color/white"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dm270">
    </android.support.v7.widget.RecyclerView>
    <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/dm034"
            android:background="@mipmap/title_down_bg"/>

    <RelativeLayout
        android:id="@+id/rl_rank_bg"
        android:layout_below="@+id/rv_rank_list"
        android:background="#7fFFFFFF"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </RelativeLayout>
    <View
            android:id="@+id/view_bg"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dm035"
            android:layout_below="@+id/rv_rank_list"
            android:background="@mipmap/report_rank_bg"/>
</RelativeLayout>